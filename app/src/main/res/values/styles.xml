<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextAppearance.Widget.Event.Toolbar.Title." parent="Theme.AppCompat">
        <item name="android:textAlignment">center</item>
        <item name="android:fontFamily">@font/khula_regular</item>
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColorSecondary">#000000</item>
        <item name="actionMenuTextColor">#000000</item>
    </style>

    <style name="TextAppearance.Widget.Event.Toolbar.Title2" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColorSecondary">#000000</item>
        <item name="actionMenuTextColor">#000000</item>
    </style>

    <style name="NavigationTheme">
        <item name="android:fontFamily">@font/khula_lightt</item>
        <item name="android:textSize">22sp</item>
    </style>

</resources>
